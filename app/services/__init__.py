"""
Services package - lazy imports to avoid circular dependencies
"""
# Import individual services directly when needed to avoid circular imports
# Example: from app.services.risk import RiskService
