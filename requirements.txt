fastapi==0.104.1
uvicorn==0.24.0
pydantic==2.5.2
pydantic-settings==2.1.0

# Database
aiomysql==0.2.0
sqlalchemy==2.0.23
alembic==1.13.0
cryptography==46.0.5

# Testing
pytest==7.4.3
pytest-asyncio==0.21.1
pytest-cov==4.1.0
pytest-mock==3.14.0

# Network Traffic Analysis
# scapy (optional for advanced packet capture)
# mitmproxy (required for real traffic capture in production)
# pyshark (optional for detailed protocol analysis)

# Utilities
python-dotenv==1.0.0
httpx==0.25.2
orjson==3.9.12
pyyaml==6.0.1

# Machine Learning (for risk assessment metrics)
scikit-learn==1.3.2

# Note: pandas and numpy are optional and not required for core functionality.
# They are commented out to avoid build issues on newer Python versions.
# Uncomment if you need data processing features:
# pandas==2.1.4
# numpy==1.26.2
